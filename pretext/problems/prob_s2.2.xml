<?xml version="1.0" encoding="UTF-8" ?>
<!-- Copyright 2018 Joel Feldman, Andrew Rechnitzer and Elyse Yeager -->
<!-- This work is licensed under the Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International License-->
<!-- https://creativecommons.org/licenses/by-nc-sa/4.0 -->
<exercises>

<exercisegroup><title><conceptual/></title>
<introduction></introduction>

<exercise>
<statement><p>
The function <m>f(x)</m> is shown. Select all options below that describe its derivative, <m>\ds\diff{f}{x}</m>:
<ul>
<li>(a) constant</li>
<li>(b) increasing</li>
<li>(c) decreasing </li>
<li>(d) always positive</li>
<li>(e) always negative</li>
</ul>
</p>

<sidebyside width="80%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{5}{3}
\draw[thick] plot[domain=-4:4](\x,{\x/5*3}) node[below right]{$y=f(x)$};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>
</statement>
<hint><p> What are the properties of <m>f'</m> when <m>f</m> is a line?
</p></hint>
<answer><p>(a), (d)
</p></answer>
<solution><p> The function shown is a line, so it has a constant slope--(a) . Since the function is always increasing, <m>f'</m> is always positive, so also (d) holds. Remark: it does not matter that the function itself is sometimes negative; the slope is always positive because the function is always increasing. Also, since the slope is constant, <m>f'</m> is neither increasing nor decreasing: it is the <em>function</em> that is increasing, not its derivative.
</p></solution>
</exercise>

<exercise>
<statement><p>
The function <m>f(x)</m> is shown. Select all options below that describe its derivative, <m>\ds\diff{f}{x}</m>:
<ul>
<li>(a) constant</li>
<li>(b) increasing</li>
<li>(c) decreasing</li>
<li>(d) always positive</li>
<li>(e) always negative</li>
</ul>
</p>

<sidebyside width="80%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{5}{3}
\draw[thick] plot[smooth] coordinates{(-4,3) (-3,2.75) (-2,2) (-1,1.75) (0,1)};
\draw[thick] plot[domain=0:4](\x,{exp(-\x)})node[below right]{$y=f(x)$};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>
</statement>
<hint><p> Be very careful not to confuse <m>f</m> and <m>f'</m>.
</p></hint>
<answer><p> (e)
</p></answer>
<solution><p> The function is always decreasing, so <m>f'</m> is always negative, option (e). However, the function alternates between being more and less steep, so <m>f'</m> alternates between increasing and decreasing several times, and no other options hold.
</p>

<p> Remark: <m>f</m> is always positive, but (d) does not hold!
</p></solution>
</exercise>

<exercise purpose="RQS">
<statement><p>
The function <m>f(x)</m> is shown. Select all options below that describe its derivative, <m>\ds\diff{f}{x}</m>:
<ul>
<li>(a) constant</li>
<li>(b) increasing</li>
<li>(c) decreasing</li>
<li>(d) always positive</li>
<li>(e) always negative</li>
</ul>
</p>

<sidebyside  width="80%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{5}{3}
\draw[thick] plot[domain=-4:4](\x,{\x*\x/25*9-3}) node[below right]{$y=f(x)$};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>
</statement>
<hint><p> Be very careful not to confuse <m>f</m> and <m>f'</m>.
</p></hint>
<answer><p> (b)
</p></answer>
<solution><p>
At the left end of the graph, <m>f</m> is decreasing rapidly, so <m>f'</m> is a strongly negative number. Then as we move towards <m>x=0</m>, <m>f</m> decreases less rapidly, so <m>f'</m> is a less strongly negative number. As we pass 0, <m>f</m> increases, so <m>f'</m> is a positive number. As we move to the right, <m>f</m> increases more and more rapidly, so <m>f'</m> is an increasing positive number. This description tells us that <m>f'</m> increases for the entire range shown. So (b) holds, but not (a) or (c). Since <m>f'</m> is negative to the left of the <m>y</m> axis, and positive to the right of it, also (d) and (e) do not hold.
</p></solution>
</exercise>

<exercise><title><fromexam>2006H</fromexam></title>
<statement><p>
State, in terms of a limit, what it means for
<m>f(x) = x^3</m> to be differentiable at <m>x = 0</m>.
</p></statement>
<answer><p>
By definition, <m>f(x) = x^3</m> is differentiable at <m>x = 0</m>
if the limit
<me>
\lim_{h\rightarrow 0}\frac{f(h)-f(0)}{h}
=\lim_{h\rightarrow 0}\frac{h^3-0}{h}
</me>
exists.
</p></answer>
<solution><p>
By definition, <m>f(x) = x^3</m> is differentiable at <m>x = 0</m>
if the limit
<me>
\lim_{h\rightarrow 0}\frac{f(h)-f(0)}{h}
=\lim_{h\rightarrow 0}\frac{h^3-0}{h}
</me>
exists.
</p></solution>
</exercise>

<exercise purpose="RQS">
<statement><p>For which values of <m>x</m> does <m>f'(x)</m> not exist?</p>

<sidebyside width="80%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{5.2}{3.2}
\draw[thin, gray, dotted] (-5,-3) grid (5,3);
\draw[thick] (-5,-2) sin (-3,1) cos (-1,2)--(3,1);
\draw (3,1) node[vertex]{};
\draw (3,-2) node[opendot]{}--(5,-2.5);
\draw (1,.2)--(1,-.2) node[below]{1};
\draw (.2,1)--(-.2,1) node[left]{1};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>
</statement>
<hint><p> The slope has to look <q>the same</q> from the left and the right.
</p></hint>
<answer><p> <m>x=-1</m> and <m>x=3</m>
</p></answer>
<solution><p>
<m>f'(-1)</m> does not exist, because to the left of <m>x=-1</m> the slope is a pretty big positive number (looks like around <m>+1</m>) and to the right the slope is <m>-1/4</m>. Since the derivative involves a limit, that limit needs to match the limit from the left and the limit from the right. The sharp angle made by the graph at <m>x=-1</m> indicates that the left and right limits do not match, so the derivative does not exist.
</p>

<p><m>f'(3)</m> also does not exist.  One way to see this is to notice that the function is discontinuous here. More viscerally, note that <m>f(3)=1</m>, so as we take secant lines with one endpoint <m>(3,1)</m>, and the other endpoint just to the right of <m>x=3</m>, we get slopes that are more and more strongly negative, as shown in the picture below. If we take the limit of the slopes of these secant lines as <m>x</m> goes to <m>3</m> from the right, we get <m>-\infty</m>. (This certainly doesn't match the slope from the left, which is <m>-\frac{1}{4}</m>.)</p>

<sidebyside width="50%">
<image><latex-image><![CDATA[\begin{tikzpicture}[scale=0.5]
\YEaxis{5.2}{3.2}
\draw[thin, gray, dotted] (-5,-3) grid (5,3);
\draw[thick] (-5,-2) sin (-3,1) cos (-1,2)--(3,1);
\draw (3,1) node[vertex]{};
\draw (3,-2) node[opendot]{}--(5,-2.5);
\draw (1,.2)--(1,-.2) node[below]{1};
\draw (.2,1)--(-.2,1) node[left]{1};
\draw[thick,orange] (3,1) --(4,-2.25) node[vertex]{};
\draw[thick,orange] (3,1) --(5,-2.5) node[vertex]{};
\draw[thick,red] (3,1) node[vertex]{}--(3.5,-2.125) node[vertex]{};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>

<p>At <m>x=-3</m>, there is some kind of <q>change</q> in the graph; however, it is a smooth curve, so the derivative exists here.
</p></solution>
</exercise>

<exercise xml:id="s2_2onesided1" purpose="RQS">
<statement><p>
Suppose <m>f(x)</m> is a function defined at <m>x=a</m> with <me>
\lim_{h \to 0^-}\frac{f(a+h)-f(a)}{h}=\lim_{h \to 0^+}\frac{f(a+h)-f(a)}{h}=1.</me>
 True or false: <m>f'(a)=1</m>.
</p></statement>
<hint><p> Use the definition of the derivative, and what you know about limits.
</p></hint>
<answer><p> True. (Contrast to Question<nbsp/><xref ref="s2_2onesided2"/>.)
</p></answer>
<solution><p>
True.
The definition of the derivative tells us that
<me>
f'(a) = \lim_{h \to 0}\dfrac{f(a+h)-f(a)}{h},</me>
 if it exists. We know from our work with limits that if both one-sided limits
</p>
<p>
<m>\ds\lim_{h \to 0^-}\frac{f(a+h)-f(a)}{h}</m> and <m>\ds\lim_{h \to 0^+}\frac{f(a+h)-f(a)}{h}</m> exist and are equal to each other, then <m>\ds \lim_{h \to 0}\dfrac{f(a+h)-f(a)}{h}</m> exists and has the same value as the one-sided limits. So, since the one-sided limits exist and are equal to one, we conclude <m>f'(a)</m> exists and is equal to one.
</p></solution>
</exercise>

<exercise xml:id="s2_2onesided2" purpose="RQS">
<statement><p>
Suppose <m>f(x)</m> is a function defined at <m>x=a</m> with
<me>
\lim_{x \to a^-}f'(x)=\lim_{x \to a^+}f'(x)=1.</me>
 True or false: <m>f'(a)=1</m>.
</p></statement>
<hint><p> Consider continuity. </p></hint>
<answer><p> In general, false. (Contrast to Question<nbsp/><xref ref="s2_2onesided1"/>.)
</p></answer>
<solution><p>
In general, this is false. The key problem that can arise is that <m>f(x)</m> might not be continuous at <m>x=1</m>.  One example is the function
<me>
f(x)=\left\{\begin{array}{ll}
x&amp;x \lt 0\\
x-1&amp;x \geq 0
\end{array}\right.
</me>
where <m>f'(x)=1</m> whenever <m>x \neq 0</m>  (so in particular, <m>\ds\lim_{x \to 0^-}f'(x)=\ds\lim_{x \to 0^+} f'(x)=1</m>) but <m>f'(0)</m> does not exist.
</p>

<p>There are two ways to see that <m>f'(0)</m> does not exist. One is to notice that <m>f</m> is not continuous at <m>x=0</m>.
</p>

<sidebyside width="66%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{3}{3}
\draw[thick] plot[domain=-3:0](\x,\x);
\draw node[opendot]{};
\draw[thick] plot[domain=0:3](\x,{\x-1}) node[right]{$y=f(x)$};
\draw (0,-1) node[vertex]{};
\end{tikzpicture}]]></latex-image></image>
</sidebyside>

<p>Another way to see that <m>f'(0)</m> does not exist is to use the definition of the derivative. Remember, in order for a limit to exist, both one-sided limits must exist. Let's consider the limit from the left. If <m>h \to 0^-</m>, then <m>h \lt 0</m>, so <m>f(h)</m> is equal to <m>h</m> (not <m>h-1</m>).
</p>

<p><md>
<mrow>
\lim_{h \to 0^-}\frac{f(0+h)-f(0)}{h}&amp;=\lim_{h \to 0^-}\frac{(h)-(-1)}{h}
</mrow><mrow>
&amp;=\lim_{h \to 0^-}\frac{h+1}{h}
</mrow><mrow>
&amp;=\lim_{h \to 0^-}1+\frac{1}{h}
</mrow><mrow>
&amp;=-\infty
</mrow>
<intertext>In particular, this limit does not exist. Since the one-sided limit does not exist,</intertext><mrow>
\lim_{h \to 0}\frac{f(0+h)-f(0)}{h}&amp;=DNE
</mrow>
</md>
and so <m>f'(0)</m> does not exist.
</p></solution>
</exercise>

<exercise purpose="RQS">
<statement><p>
Suppose <m>s(t)</m> is a function, with <m>t</m> measured in seconds, and <m>s</m> measured in metres. What are the units of <m>s'(t)</m>?
</p></statement>
<hint><p>
Look at the definition of the derivative. Your answer will be a fraction.
</p></hint>
<answer><p> metres per second
</p></answer>
<solution><p>
Using the definition of the derivative,
<me>
s'(t)=\lim_{h \to 0}\frac{s(t+h)-s(t)}{h}</me>
</p>

<p>The units of the numerator are meters, and the units of the denominator are seconds (since the denominator comes from the change in the <em>input</em> of the function). So, the units of <m>s'(t)</m> are metres per second.
</p>

<p>Remark: we learned that the derivative of a position function gives velocity. In this example, the position is given in metres, and the velocity is measured in metres per second.
</p></solution>
</exercise>
</exercisegroup>

<exercisegroup><title><procedural/></title>
<introduction></introduction>

<exercise>
<statement><p>Use the definition of the derivative to find the equation of the tangent line to the curve <m>y(x)=x^3+5</m> at the point <m>(1,6)</m>.
</p></statement>
<hint><p> You need a point (given), and a slope (derivative).
</p></hint>
<answer><p> <m>y-6=3(x-1)</m>, or <m>y=3x +3</m>
</p></answer>
<solution><p>
We can use point-slope form to get the equation of the line, if we have a point and its slope. The point is given: <m>(1,6)</m>. The slope is the derivative:
<md>
<mrow>
y'(1)&amp;=\lim_{h \rightarrow 0}\frac{y(1+h)-y(1)}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{[(1+h)^3+5]-[1^3+5]}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{[1+3h+3h^2+h^3+5]-[1+5]}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{3h+3h^2+h^3}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}{3+3h+h^2}
</mrow><mrow>
&amp;=3
</mrow>
</md>
So our slope is 3, which gives a line of equation
<m>y-6=3(x-1)</m>.
</p></solution>
</exercise>

<exercise>
<statement><p>Use the definition of the derivative to find the derivative of
<m>f(x)=\frac{1}{x}</m>.
</p></statement>
<hint><p> You'll need to add some fractions.
</p></hint>
<answer><p> <m>\dfrac{-1}{x^2}</m>
</p></answer>
<solution><p>
We set up the definition of the derivative.
<md>
<mrow>
f'(x)&amp;=\lim_{h \rightarrow 0}\frac{f(x+h)-f(x)}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{\frac{1}{x+h}-\frac{1}{x}}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{\frac{x}{x(x+h)}-\frac{x+h}{x(x+h)}}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{\frac{x-(x+h)}{x(x+h)}}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{\frac{-h}{x(x+h)}}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{-1}{x(x+h)}
</mrow><mrow>
&amp;=\frac{-1}{x^2}
</mrow>
</md>
</p></solution>
</exercise>

<exercise><title><fromexam>2007H</fromexam></title>
<statement><p>
Let <m>f(x) = x|x|</m>.
Using the definition of the derivative, show that
<m>f(x)</m> is differentiable at <m>x = 0</m>.
</p></statement>
<hint><p>
You don't have to take the limit from the left and right separately--things will cancel nicely.
</p></hint>
<answer><p>
 By definition
<me>
f'(0)=\lim_{h\rightarrow 0}\frac{f(h)-f(0)}{h}
     =\lim_{h\rightarrow 0}\frac{h|h|}{h}
     =\lim_{h\rightarrow 0}|h|=0
</me>
In particular, the limit exists, so the derivative exists (and is equal to zero).
</p></answer>
<solution><p>
 By definition
<me>
f'(0)=\lim_{h\rightarrow 0}\frac{f(h)-f(0)}{h}
     =\lim_{h\rightarrow 0}\frac{h|h|}{h}
     =\lim_{h\rightarrow 0}|h|=0
</me>
In particular, the limit exists, so the derivative exists (and is equal to zero).
</p></solution>
</exercise>

<exercise purpose="RQS"><title><fromexam>1997A</fromexam></title>
<statement><p>Use the definition of the derivative to compute the derivative
of the function <m>f(x)=\frac{2}{x+1}</m>.
</p></statement>
<hint><p> You might have to add fractions.
</p></hint>
<answer><p> <m>\dfrac{-2}{(x+1)^2}</m>
</p></answer>
<solution><p>
We set up the definition of the derivative.
<md>
<mrow>
f'(x)&amp;=\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}
=\lim_{h\rightarrow 0}\frac{1}{h}\Big(\frac{2}{x+h+1}
-\frac{2}{x+1}\Big)
</mrow><mrow>
\amp=\lim_{h\rightarrow 0}\frac{2}{h}\ \frac{(x+1)-(x+h+1)}{(x+h+1)(x+1)}
</mrow><mrow>
&amp;=\lim_{h\rightarrow 0}\frac{2}{h}\ \frac{-h}{(x+h+1)(x+1)}
</mrow><mrow>
\amp=\lim_{h\rightarrow 0}\frac{-2}{(x+h+1)(x+1)}
</mrow><mrow>
\amp=\frac{-2}{(x+1)^2}
</mrow>
</md>
</p></solution>
</exercise>

<exercise><title><fromexam>1996D</fromexam></title>
<statement><p>
Use the definition of the derivative to compute the derivative
of the function <m>f(x)=\frac{1}{x^2+3}</m>.
</p></statement>
<answer><p> <m>\dfrac{-2x}{[x^2+3]^2}</m>
</p></answer>
<solution><p>
<md>
<mrow>
f'(x)&amp;=\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}
=\lim_{h\rightarrow 0}\frac{1}{h}\Big(\frac{1}{(x+h)^2+3}
-\frac{1}{x^2+3}\Big)
</mrow><mrow>
\amp=\lim_{h\rightarrow 0}\frac{1}{h}\frac{x^2-(x+h)^2}{[(x+h)^2+3][x^2+3]}
</mrow><mrow>
&amp;=\lim_{h\rightarrow 0}\frac{1}{h}\frac{-2xh-h^2}{[(x+h)^2+3][x^2+3]}
</mrow><mrow>
\amp=\lim_{h\rightarrow 0}\frac{-2x-h}{[(x+h)^2+3][x^2+3]}
</mrow><mrow>
\amp=\frac{-2x}{[x^2+3]^2}
</mrow>
</md>
</p></solution>
</exercise>

<exercise>
<statement><p>Use the definition of the derivative to find the slope of the tangent line to the curve <m>f(x)=x\log_{10}(2x+10)</m> at the point <m>x=0</m>.
</p></statement>
<hint><p> Your limit should be easy.
</p></hint>
<answer><p> 1
</p></answer>
<solution><p> The slope of the tangent line is the derivative.
We set this up using the same definition of the derivative that we always do. This limit is hard to take for general <m>x</m>, but easy when <m>x=0</m>.
<md>
<mrow>
f'(0)&amp;=\lim_{h \rightarrow 0} \frac{f(0+h)-f(0)}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} \frac{h\log_{10}(2h+10)-0}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} \log_{10}(2h+10)=\log_{10}(10)=1
</mrow>
</md>
So, the slope of the tangent line is 1.
</p></solution>
</exercise>

<exercise><title><fromexam>1998H</fromexam></title>
<statement><p>
Compute the derivative of <m>f(x)=\frac{1}{x^2}</m> directly
from the definition.
</p></statement>
<hint><p> Add fractions.
</p></hint>
<answer><p> <m>f'(x)=-\dfrac{2}{x^3}</m>
</p></answer>
<solution><p>
<md>
<mrow>
f'(x)&amp;=\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}
=\lim_{h\rightarrow 0}\frac{\frac{1}{(x+h)^2}-\frac{1}{x^2}}{h}
=\lim_{h\rightarrow 0}\frac{x^2-(x+h)^2}{(x+h)^2x^2h}
</mrow><mrow>
\amp=\lim_{h\rightarrow 0}\frac{-2xh-h^2}{(x+h)^2x^2h}
=\lim_{h\rightarrow 0}\frac{-2x-h}{(x+h)^2x^2}
=\frac{-2x}{x^4}
</mrow><mrow>
\amp=-\frac{2}{x^3}
</mrow>
</md>
</p></solution>
</exercise>

<exercise purpose="RQS"><title><fromexam>2006H</fromexam></title>
<statement><p>
Find the values of the constants <m>a</m> and <m>b</m> for which
<md>
<mrow>
f(x) = \left\{
\begin{array}{lc}
	 x^2 &amp; x\le 2
</mrow><mrow>
               ax + b   &amp; x  \gt  2
               \end{array}\right.
</mrow>
</md>
is differentiable everywhere.
</p>

<p>Remark: In the text, you have already learned the derivatives of <m>x^2</m> and <m>ax+b</m>. In this question, you are only asked to find the values of <m>a</m> and <m>b</m><mdash/>not to justify how you got them<mdash/>so you don't have to use the definition of the derivative. However, on an exam, you might be asked to justify your answer, in which case you would show how to differentiate the two branches of <m>f(x)</m> using the definition of a derivative.
</p></statement>
<hint><p>
For <m>f</m> to be differentiable at <m>x=2</m>, two things must be true: it must be continuous at <m>x=2</m>, and the derivative from the right must equal the derivative from the left.
</p></hint>
<answer><p>
<m>a=4</m>, <m>b=-4</m>
</p></answer>
<solution><p>
When <m>x</m> is not equal to 2, then the function is differentiable-- the only place we have to worry about is when <m>x</m> is exactly 2.
</p>

<p>In order for <m>f</m> to be differentiable at <m>x=2</m>, it must also
be continuous at <m>x=2</m>. This forces <m>x^2\big|_{x=2}=\big[ax+b\big]_{x=2}</m>
or <me>
2a+b=4.</me>
</p>

<p>In order for a limit to exist, the left- and right-hand limits must exist and be equal to each other. Since a derivative is a limit, in order for <m>f</m> to be differentiable at <m>x=2</m>, the left hand
derivative of <m>ax+b</m> at <m>x=2</m> must be
the same as the right hand derivative of <m>x^2</m> at <m>x=2</m>.
Since <m>ax+b</m> is a line, its derivative is <m>a</m> everywhere. We've already seen the derivative of <m>x^2</m> is <m>2x</m>, so we need
<me>
a=2x\big|_{x=2}=4.</me>
</p>

<p>So, the values of <m>a</m> and <m>b</m> that makes <m>f</m> differentiable everywhere are <m>a=4</m> and <m>b=-4</m>.
</p></solution>
</exercise>

<exercise><title><fromexam>2009H</fromexam></title>
<statement><p>
Use the definition of the derivative to compute
<m>f'(x)</m> if <m>f(x) = \sqrt{1 + x}</m>. Where does <m>f'(x)</m> exist?
</p></statement>
<hint><p>
After you plug in <m>f(x)</m> to the definition of a derivative, you'll want to multiply and divide by the conjugate <m>\sqrt{1+x+h}+\sqrt{1+x}</m>.
</p></hint>
<answer><p> <m>f'(x)=\dfrac{1}{2\sqrt{1+x}}</m> when <m>x \gt -1</m>; <m>f'(x)</m> does not exist when <m>x \leq -1</m>.
</p></answer>
<solution><p>
We plug in <m>f(x)</m> to the definition of a derivative. To evaluate the limit, we multiply and divide by the conjugate of the numerator, then simplify.
<md>
<mrow>
\amp f'(x)=\lim\limits_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}
=\lim\limits_{h\rightarrow 0}\frac{\sqrt{1+x+h}-\sqrt{1+x}}{h}
</mrow><mrow>
&amp;\hskip0.5in=\lim\limits_{h\rightarrow 0}\frac{\sqrt{1+x+h}-\sqrt{1+x}}{h}
\left(\frac{\sqrt{1+x+h}+\sqrt{1+x}}{\sqrt{1+x+h}+\sqrt{1+x}}\right)
</mrow><mrow>
&amp;\hskip0.5in=\lim\limits_{h\rightarrow 0}\frac{(1+x+h)-(1+x)}{h(\sqrt{1+x+h}+\sqrt{1+x})}
</mrow><mrow>
&amp;\hskip0.5in=\lim\limits_{h\rightarrow 0}\frac{h}{h(\sqrt{1+x+h}+\sqrt{1+x})}
</mrow><mrow>
&amp;\hskip0.5in=\lim\limits_{h\rightarrow 0}\frac{1}{\sqrt{1+x+h}+\sqrt{1+x}}
</mrow><mrow>
&amp;\hskip0.5in=\frac{1}{\sqrt{1+x+0}+\sqrt{1+x}}=\frac{1}{2\sqrt{1+x}}
</mrow>
</md>
</p>

<p>The domain of the function is <m>[-1,\infty)</m>. In particular, <m>f(x)</m> is defined when <m>x=-1</m>. However, <m>f'(x)</m> is not defined when <m>x=-1</m>, so <m>f'(x)</m> only exists over <m>(-1,\infty)</m>.
</p>

<p>Remark: <m>\ds\lim_{x \to -1^+}f'(x)=\infty</m>, so the tangent line to <m>f(x)</m> at the point <m>x=-1</m> has a vertical slope.
</p></solution>
</exercise>
</exercisegroup>

<exercisegroup><title><application/></title>
<introduction></introduction>

<exercise>
<statement><p> Use the definition of the derivative to find the velocity of an object whose position is given by the function <m>s(t)=t^4-t^2</m>.
</p></statement>
<hint><p> From Section<nbsp/><xref ref="sec_velocity"/>,
compare the definition of velocity to the definition of a derivative. When you're finding the derivative, you'll need to cancel a lot on the numerator, which you can do by expanding the polynomials.
</p></hint>
<answer><p> <m>v(t)=4t^3-2t</m>
</p></answer>
<solution><p>
From Section<nbsp/><xref ref="sec_velocity"/>,
we see that the velocity is exactly the derivative.
<md>
<mrow>
v(t)&amp;=\lim_{h \rightarrow 0}\frac{s(t+h)-s(t)}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{(t+h)^4-(t+h)^2-t^4+t^2}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{(t^4+4t^3h+6t^2h^2+4th^3+h^4)-(t^2+2th+h^2)-t^4+t^2}{h}

</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}\frac{4t^3h+6t^2h^2+4th^3+h^4-2th-h^2}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0}4t^3+6t^2h+4th^2+h^3-2t-h
</mrow><mrow>
&amp;=4t^3-2t
</mrow>
</md>
So, the velocity is given by <m>v(t)=4t^3-2t</m>.
</p></solution>
</exercise>

<exercise purpose="RQS"><title><fromexam>2015Q</fromexam></title>
<statement><p>
 Determine whether the derivative of following function exists at
<m>x=0</m>.
<md>
<mrow>
f(x) &amp;=\begin{cases}
  x \cos x &amp; \text{ if }  x\ge 0
</mrow><mrow>
  \sqrt{x^2+x^4} &amp; \text{ if } x \lt  0
\end{cases}
</mrow>
</md>
You must justify your answer using the definition of a derivative.
</p></statement>
<hint><p> You'll need to look at limits from the left and right. The fact that <m>f(0)=0</m> is useful for your computation. Recall that if <m>x \lt 0</m> then <m>\sqrt{x^2}=|x|=-x</m>.
</p></hint>
<answer><p> No, it does not.
</p></answer>
<solution><p>
The function is differentiable at <m>x=0</m> if the following limit:
<me>\lim_{x\to 0}\frac{f(x)-f(0)}{x-0} = \lim_{x\to 0}\frac{f(x)-0}{x}=\lim_{x\to
0} \frac{f(x)}{x}</me>
exists (note that we used the fact that <m>f(0)=0</m> as per the definition of the
first branch which includes the point <m>x=0</m>). We start by computing the left limit. For this computation, recall that if <m>x \lt 0</m> then <m>\sqrt{x^2}=|x|=-x</m>.
<md>
<mrow>
\lim_{x\to 0^-}\frac{f(x)}{x}\amp
=\lim_{x\to 0^-}\frac{\sqrt{x^2+x^4}}{x}
=\lim_{x\to 0^-} \frac{\sqrt{x^2}\sqrt{1+x^2}}{x}
</mrow><mrow>
\amp=\lim_{x \rightarrow 0}\frac{-x\sqrt{1+x^2}}{x}=-1
</mrow>
</md>
Now, from the right:
<me>\lim_{x\to 0^+}\frac{x\cos x}{x}=\lim_{x\to
0^+}\cos x = 1.</me>
Since the limit from the left does not equal the limit from the right, the derivative does not exist at <m>x=0</m>.
</p></solution>
</exercise>

<exercise><title><fromexam>2015Q</fromexam></title>
<statement><p>
 Determine whether the derivative of the following function exists at
<m>x=0</m>
<md>
<mrow>
f(x) &amp;=\begin{cases}
  x \cos x &amp; \text{ if }  x\le 0
</mrow><mrow>
  \sqrt{1+x}-1 &amp; \text{ if } x \gt  0
\end{cases}
</mrow>
</md>
You must justify your answer using the definition of a derivative.
</p></statement>
<hint><p> You'll need to look at limits from the left and right. The fact that <m>f(0)=0</m> is useful for your computation.
</p></hint>
<answer><p> No, it does not.
</p></answer>
<solution><p>
The function is differentiable at <m>x=0</m> if the following limit:
<me>\lim_{x\to 0}\frac{f(x)-f(0)}{x-0} = \lim_{x\to 0}\frac{f(x)-0}{x}=\lim_{x\to
0} \frac{f(x)}{x}</me>
exists (note that we used the fact that <m>f(0)=0</m> as per the definition of the
first branch which includes the point <m>x=0</m>).
</p>

<p>We start by computing the left limit.
<md>
<mrow>
\lim_{x\to 0^-}\frac{f(x)}{x}=\lim_{x\to 0^-} \frac{x\cos x}{x}
=\lim_{x\to 0^-} \cos x = 1.
</mrow>
</md>
Now, from the right:
<md>
<mrow>
\lim_{x\to 0^+}\frac{\sqrt{1+x}-1}{x}
&amp;= \lim_{x\to 0^+}\frac{\sqrt{1+x}-1}{x} \cdot \frac{\sqrt{1+x}+1}{\sqrt{1+x}+1}
</mrow><mrow>
&amp;= \lim_{x\to 0^+} \frac{1+x-1}{x(\sqrt{1+x}+1)}
= \lim_{x\to 0^+} \frac{1}{\sqrt{1+x}+1} = \frac{1}{2}
</mrow>
</md>
Since the limit from the left does not equal the limit from the right, the derivative
does
not exist at <m>x=0</m>.
</p></solution>
</exercise>

<exercise><title><fromexam>2015Q</fromexam></title>
<statement><p>
Determine whether the derivative of the following function exists at
<m>x=0</m>
<md>
<mrow>
f(x) &amp;=\begin{cases}
  x^3-7x^2 &amp; \text{ if }  x\le 0
</mrow><mrow>
  x^3 \cos\left(\frac{1}{x}\right) &amp; \text{ if } x \gt  0
\end{cases}
</mrow>
</md>
You must justify your answer using the definition of a derivative.
</p></statement>
<hint><p> You'll need to look at limits from the left and right. The fact that <m>f(0)=0</m> is useful for your computation.
</p></hint>
<answer><p> Yes, it is.
</p></answer>
<solution><p>
The function is differentiable at <m>x=0</m> if the following limit:
<md>
<mrow>
\lim_{x\to 0}\frac{f(x)-f(0)}{x-0} = \lim_{x\to 0}\frac{f(x)-0}{x}=\lim_{x\to 0}
\frac{f(x)}{x}
</mrow>
</md>
exists (note that we used the fact that <m>f(0)=0</m> as per the definition of the first branch
which includes the point <m>x=0</m>). We compute left and right limits; so
<md>
<mrow>
\lim_{x\to 0^-}\frac{f(x)}{x}=\lim_{x\to 0^-}\frac{x^3-7x^2}{x}=\lim_{x\to 0^-}
x^2-7x=0
</mrow>
</md>
and
<md>
<mrow>
\lim_{x\to 0^+}\frac{x^3\cos\left(\frac{1}{x}\right)}{x}=\lim_{x\to 0^+}x^2\cdot
\cos\left(\frac{1}{x}\right).
</mrow>
</md>
This last limit equals <m>0</m> by the Squeeze Theorem since
<md>
<mrow>
-1\le \cos\left(\frac{1}{x}\right)\le 1
</mrow>
</md>
and so,
<md>
<mrow>
-x^2\le x^2\cdot \cos\left(\frac{1}{x}\right)\le x^2,
</mrow>
</md>
where in these inequalities we used the fact that <m>x^2\ge 0</m>. Finally, since <m>\lim_{x\to 0^+}-x^2=\lim_{x\to 0^+}x^2=0</m>, the Squeeze Theorem
yields that also <m>\lim_{x\to 0^+}x^2\cos\left(\frac{1}{x}\right) =0</m>, as claimed.
</p>

<p>Since the left and right limits match (they're both equal to <m>0</m>), we conclude that indeed
<m>f(x)</m> is differentiable at <m>x=0</m> (and its derivative at <m>x=0</m> is actually equal to <m>0</m>).
</p></solution>
</exercise>

<exercise><title><fromexam>2015Q</fromexam></title>
<statement><p>
Determine whether the derivative of the following function exists at
<m>x=1</m>
<md>
<mrow>
f(x) &amp;=\begin{cases}
  4x^2-8x+4 &amp; \text{ if }  x\le 1
</mrow><mrow>
  (x-1)^2\sin\left(\dfrac{1}{x-1}\right) &amp; \text{ if } x \gt  1
\end{cases}
</mrow>
</md>
You must justify your answer using the definition of a derivative.
</p></statement>
<hint><p> You'll need to look at limits from the left and right. The fact that <m>f(1)=0</m> is useful for your computation.
</p></hint>
<answer><p>
Yes, it is.
</p></answer>
<solution><p>
The function is differentiable at <m>x=1</m> if the following limit:
<me>\lim_{x\to 1}\frac{f(x)-f(1)}{x-1} = \lim_{x\to 1}\frac{f(x)-0}{x-1}=\lim_{x\to 1}
\frac{f(x)}{x-1}</me>
exists (note that we used the fact that <m>f(1)=0</m> as per the definition of the first branch which includes the point <m>x=0</m>). We compute left and right limits; so
<md>
<mrow>
\lim_{x\to 1^-}\frac{f(x)}{x-1}
\amp=\lim_{x\to 1^-}\frac{4x^2-8x+4}{x-1}
=\lim_{x\rightarrow 1^-}\frac{4(x-1)^2}{x-1} 
</mrow><mrow>
\amp=\lim_{x\to 1^-} 4(x-1)=0
</mrow>
</md>
and
<me>\lim_{x\to 1^+}\frac{(x-1)^2\sin\left(\frac{1}{x-1}\right)}{x-1}=\lim_{x\to 1^+}(x-1)\cdot \sin\left(\frac{1}{x-1}\right).</me>
This last limit equals <m>0</m> by the Squeeze Theorem since
<me>-1\le \sin\left(\frac{1}{x-1}\right)\le 1</me>
and so,
<me> -(x-1)\le (x-1)\cdot \sin\left(\frac{1}{x-1}\right)\le x-1,</me>
where in these inequalities we used the fact that <m>x\to 1^+</m> yields positive values for
<m>x-1</m>. Finally, since <m>\lim_{x\to 1^+}-x+1=\lim_{x\to 1^+}x-1=0</m>, the Squeeze Theorem
yields that also <m>\lim_{x\to 1^+}(x-1)\sin\left(\frac{1}{x-1}\right) =0</m>, as claimed.
</p>

<p>Since the left and right limits match (they're both equal to <m>0</m>), we conclude that indeed <m>f(x)</m> is differentiable at <m>x=1</m> (and its derivative at <m>x=1</m> is actually equal to <m>0</m>).
</p></solution>
</exercise>

<exercise purpose="RQS"><title></title>
<statement><p>
Sketch a function <m>f(x)</m> with <m>f'(0)=-1</m> that takes the following values:
</p>

<sidebyside>
<tabular>
<row>
<cell><m>\mathbf{x}</m></cell><cell><m>-1</m></cell><cell><m>-\frac{1^{ }}{2_{ }}</m></cell><cell><m>-\frac{1}{4}</m></cell><cell><m>-\frac{1}{8}</m>
</cell><cell><m>0</m>
</cell><cell><m>\frac{1}{8}</m></cell><cell><m>\frac{1}{4}</m></cell><cell><m>\frac{1}{2}</m></cell><cell><m>1</m></cell>
</row><row>
<cell><m>\mathbf{f(x)}</m></cell><cell><m>-1</m></cell><cell><m>-\frac{1^{ }}{2_{ }}</m></cell><cell><m>-\frac{1}{4}</m></cell><cell><m>-\frac{1}{8}</m>
</cell><cell><m>0</m>
</cell><cell><m>\frac{1}{8}</m></cell><cell><m>\frac{1}{4}</m></cell><cell><m>\frac{1}{2}</m></cell><cell><m>1</m></cell>
</row>
</tabular>
</sidebyside>

<p>Remark: you can't always guess the behaviour of a function from its points, even if the points seem to be making a clear pattern.
</p></statement>
<hint><p> There's lots of room between <m>0</m> and <m>\frac{1}{8}</m>; see what you can do with it.
</p></hint>
<answer><p> Many answers are possible; here is one.</p>

<sidebyside width="66%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{4.2}{4.2}
\foreach \x in {1,1/2,1/4,1/8,0}{
	\draw (4*\x,4*\x) node[vertex]{};
	\draw (-\x*4,-\x*4) node[vertex]{};}
\draw (4,.2)--(4,-.2) node[below]{1};
\draw (.2,4)--(-.2,4) node[left]{1};
\draw[thick] (-4,-4)--(-1/2,-1/2) (1/2,1/2)--(4,4);
\draw[thick] (-1/2,-1/2) sin (-1/4,1/4) cos (0,0) sin (1/4,-1/4) cos (1/2,1/2);
\end{tikzpicture}]]></latex-image></image>
</sidebyside>
</answer>
<solution><p>Many answers are possible; here is one.</p>

<sidebyside width="66%">
<image><latex-image><![CDATA[\begin{tikzpicture}
\YEaxis{4.2}{4.2}
\foreach \x in {1,1/2,1/4,1/8,0}{
	\draw (4*\x,4*\x) node[vertex]{};
	\draw (-\x*4,-\x*4) node[vertex]{};}
\draw (4,.2)--(4,-.2) node[below]{1};
\draw (.2,4)--(-.2,4) node[left]{1};
\draw[thick] (-4,-4)--(-1/2,-1/2) (1/2,1/2)--(4,4);
\draw[thick] (-1/2,-1/2) sin (-1/4,1/4) cos (0,0) sin (1/4,-1/4) cos (1/2,1/2);
\end{tikzpicture}]]></latex-image></image>
</sidebyside>

<p>The key is to realize that the few points you're given suggest a pattern, but don't guarantee it. You only know nine points; anything can happen in between.
</p></solution>
</exercise>

<exercise>
<statement><p>Let <m>p(x)=f(x)+g(x)</m>, for some functions <m>f</m> and <m>g</m> whose derivatives exist. Use limit laws and the definition of a derivative to show that <m>p'(x)=f'(x)+g'(x)</m>.
</p>

<p>Remark: this is called the sum rule, and we'll learn more about it in Lemma<nbsp/><xref ref="thm_DIFFaddsub"/>.
</p></statement>
<hint><p> Set up your usual limit, then split it into two pieces
</p></hint>
<answer><p><md>
<mrow>
p'(x) &amp;= \lim_{h \rightarrow 0} \frac{p(x+h)-p(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0} \frac{f(x+h)+g(x+h)-f(x)-g(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0} \frac{f(x+h)-f(x)+g(x+h)-g(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0}\left[\frac{f(x+h)-f(x)}{h}+
\frac{g(x+h)-g(x)}{h}\right]
</mrow><mrow>
(*)&amp;= \left[\lim_{h \rightarrow 0} \frac{f(x+h)-f(x)}{h}\right]+ \left[\lim_{h \rightarrow 0}
\frac{g(x+h)-g(x)}{h}\right]
</mrow><mrow>
&amp;= f'(x)+g'(x)
</mrow>
</md>
At step (<m>*</m>), we use the limit law that <m>\displaystyle\lim_{x \rightarrow a}\left[ F(x)+G(x)\right] = \displaystyle\lim_{x \rightarrow a} F(x)+\displaystyle\lim_{x \rightarrow a}G(x)</m>, as long as  <m>\displaystyle\lim_{x \rightarrow a} F(x)</m> and <m>\displaystyle\lim_{x \rightarrow a}G(x)</m> exist. Because the problem states that <m>f'(x)</m> and <m>g'(x)</m> exist, we know that <m>\displaystyle\lim_{h \rightarrow 0} \frac{f(x+h)-f(x)}{h}</m> and <m>\displaystyle\lim_{h \rightarrow 0}
\frac{g(x+h)-g(x)}{h}</m> exist, so our work is valid.
</p></answer>
<solution><p>
<md>
<mrow>
p'(x) &amp;= \lim_{h \rightarrow 0} \frac{p(x+h)-p(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0} \frac{f(x+h)+g(x+h)-f(x)-g(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0} \frac{f(x+h)-f(x)+g(x+h)-g(x)}{h}
</mrow><mrow>
&amp;= \lim_{h \rightarrow 0} \left[\frac{f(x+h)-f(x)}{h}+
\frac{g(x+h)-g(x)}{h}\right]
</mrow><mrow>
(*)&amp;= \left[\lim_{h \rightarrow 0} \frac{f(x+h)-f(x)}{h}\right]+ \left[\lim_{h \rightarrow 0}
\frac{g(x+h)-g(x)}{h}\right]
</mrow><mrow>
&amp;= f'(x)+g'(x)
</mrow>
</md>
At step (<m>*</m>), we use the limit law that <m>\displaystyle\lim_{x \rightarrow a}\left[ F(x)+G(x)\right] = \displaystyle\lim_{x \rightarrow a} F(x)+\displaystyle\lim_{x \rightarrow a}G(x)</m>, as long as  <m>\displaystyle\lim_{x \rightarrow a} F(x)</m> and <m>\displaystyle\lim_{x \rightarrow a}G(x)</m> exist. Because the problem states that <m>f'(x)</m> and <m>g'(x)</m> exist, we know that <m>\displaystyle\lim_{h \rightarrow 0} \frac{f(x+h)-f(x)}{h}</m> and <m>\displaystyle\lim_{h \rightarrow 0}
\frac{g(x+h)-g(x)}{h}</m> exist, so our work is valid.
</p></solution>
</exercise>

<exercise>
<statement><p>Let <m>f(x)=2x</m>, <m>g(x)=x</m>, and <m>p(x)=f(x) \cdot g(x)</m>.
<ol label="a">
<li xml:id="s2_2prod1"> Find <m>f'(x)</m> and <m>g'(x)</m>.
</li>
<li xml:id="s2_2prod2"> Find <m>p'(x)</m>.
</li>
<li xml:id="s2_2prod3"> Is <m>p'(x)=f'(x) \cdot g'(x)</m>?
</li>
</ol>
In Theorem<nbsp/><xref ref="thm_DIFFprodRule"/>, you'll learn a rule for calculating the derivative of a product of two functions.
</p></statement>
<hint><p> You don't need the definition of the derivative for a line.</p></hint>
<answer><p>
<xref ref="s2_2prod1"/> <m>f'(x)=2</m> and <m>g'(x)=1</m>
</p>
<p><xref ref="s2_2prod2"/> <m>p'(x)=4x</m></p>
<p><xref ref="s2_2prod3"/> no
</p></answer>
<solution><p>
<xref ref="s2_2prod1"/>
Since <m>y=f(x)=2x</m> and <m>y=g(x)=x</m> are straight lines, we don't need the definition of the derivative (although you can use it if you like). <m>f'(x)=2</m> and <m>g'(x)=1</m>.
</p>

<p><xref ref="s2_2prod2"/> <m>p(x)=2x^2</m>, so <m>p(x)</m> is not a line: we use the definition of a derivative to find <m>p'(x)</m>.
<md>
<mrow>
p'(x)&amp;=\lim_{h \rightarrow 0} \frac{p(x+h)-p(x)}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} \frac{2(x+h)^2-2x^2}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} \frac{2x^2+4xh+2h^2-2x^2}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} \frac{4xh+2h^2}{h}
</mrow><mrow>
&amp;=\lim_{h \rightarrow 0} {4x+2h}
</mrow><mrow>&amp;=4x
</mrow>
</md>
</p>

<p><xref ref="s2_2prod3"/> No, <m>p'(x) = 4x \ne 2\cdot 1 = f'(x)\cdot g'(x)</m>. In general, the derivative of a product is not the same as the derivative of the functions being multiplied.
</p></solution>
</exercise>

<exercise purpose="RQS"><title><fromexam>2006H</fromexam></title>
<statement><p>
There are two distinct straight lines that pass
through the point <m>(1,-3)</m> and are tangent to the curve <m>y = x^2</m>.
Find equations for these two lines.
</p>

<p>Remark: the point <m>(1,-3)</m> does not lie on the curve <m>y=x^2</m>.
</p></statement>
<hint><p>
A generic point on the curve has coordinates <m>(\alpha, \alpha^2)</m>. In terms of <m>\alpha</m>, what is the equation of the tangent line to the curve at the point <m>(\alpha, \alpha^2)</m>? What does it mean for <m>(1,-3)</m> to be on that line?
</p></hint>
<answer><p>
<m>y=6x-9</m> and <m>y=-2x-1</m>
</p></answer>
<solution><p>
We know that <m>y'=2x</m>. So, if we choose a point <m>(\alpha,\alpha^2)</m> on the curve <m>y=x^2</m>,
then the tangent line to the curve at that point has slope <m>2\alpha</m>. That is, the tangent line has equation
<md>
<mrow>
(y-\alpha^2)&amp;=2\alpha(x-\alpha)
</mrow><mrow>
\mbox{simplified, } \qquad y&amp;=(2\alpha)x-\alpha^2
</mrow>
<intertext>So, if <m>(1,-3)</m> is on the tangent line, then</intertext><mrow>
-3&amp;=(2\alpha)(1)-\alpha^2
</mrow><mrow>
\iff\qquad 0&amp;=\alpha^2-2\alpha-3
</mrow><mrow>
\iff\qquad 0&amp;=(\alpha-3)(\alpha+1)
</mrow><mrow>
\iff\qquad \alpha&amp;=3, \quad\mbox{or}\qquad \alpha=-1.
</mrow>
<intertext>So, the tangent lines  <m>y=(2\alpha)x-\alpha^2</m> are</intertext><mrow>
y&amp;=6x-9 \quad\mbox{and}\quad y=-2x-1.
</mrow>
</md>
</p></solution>
</exercise>

<exercise><title><fromexam>2009H</fromexam></title>
<statement><p>
 For which values of <m>a</m> is the function
<me>
f(x) =\left\{\begin{array}{ll}
	0 &amp; x\le 0\\
             x^a \sin\frac{1}{x} &amp; x  \gt  0\end{array}\right.
</me>
</p>

<p>differentiable at 0?
</p></statement>
<hint><p>
Remember for a constant <m>n</m>, <me>
\ds\lim_{h \to 0} h^{n} = \left\{\begin{array}{ll}
0&amp;n \gt 0\\
1&amp;n=0\\
DNE&amp;n \lt 0
\end{array}\right.</me>
</p>

</hint>
<answer><p> <m>a \gt 1</m>
</p></answer>
<solution><p>
Using the definition of the derivative, <m>f</m> is differentiable at <m>0</m> if and only if
<md>
<mrow>
\lim_{h \to 0}\frac{f(h)-f(0)}{h}&amp;
</mrow>
<intertext>exists. In particular, this means <m>f</m> is differentiable at <m>0</m> if and only if both one-sided limits exist and are equal to each other. </intertext><mrow></mrow>
<intertext>When <m>h \lt 0</m>, <m>f(h)=0</m>, so</intertext><mrow>
\lim_{h \to 0^-}\frac{f(h)-f(0)}{h}&amp;=\lim_{h \to 0^-}\frac{0-0}{h}=0
</mrow>
<intertext>So, <m>f</m> is differentiable at <m>x=0</m> if and only if</intertext><mrow>
\lim_{h \to 0^+}\frac{f(h)-f(0)}{h}&amp;=0.
</mrow>
<intertext>To evaluate the  limit above, we note <m>f(0)=0</m> and, when <m>h \gt 0</m>, <m>f(h)=h^a\sin\left(\frac{1}{h}\right)</m>, so</intertext><mrow>
\lim_{h \to 0^+}\frac{f(h)-f(0)}{h}&amp;=\lim_{h \to 0^+}\frac{h^a\sin\left(\frac{1}{h}\right)}{h}
</mrow><mrow>
&amp;=\lim_{h \to 0^+}h^{a-1}\sin\left(\frac{1}{h}\right)
</mrow>
</md>
We will spend the rest of this solution evaluating the limit above for different values of <m>a</m>, to find when it is equal to zero and when it is not. Let's consider the different values that could be taken by <m>h^{a-1.}</m>
<ul>
<li><p> If <m>a=1</m>, then <m>a-1=0</m>, so <m>h^{a-1}=h^0=1</m> for all values of <m>h</m>. Then
<me>
\lim_{h \to 0^+}h^{a-1}\sin\left(\frac{1}{h}\right)=\lim_{h \to 0^+}\sin\left(\frac{1}{h}\right)=DNE</me>
</p>

<p>(Recall that the function <m>\sin\left(\frac{1}{x}\right)</m> oscillates faster and faster as <m>x</m>
goes to 0. We first saw this behaviour in Example<nbsp/><xref ref="eg_sinpix"/>.)</p>
</li>
<li><p> If <m>a \lt 1</m>, then <m>a-1 \lt 0</m>, so <m>\ds\lim_{h \to 0^+}h^{a-1}=\infty</m>. (Since we have a negative exponent, we are in effect <em>dividing  by</em> a smaller and smaller positive number. For example, if <m>a=\frac{1}{2}</m>, then <m>\ds\lim_{h \to 0^+}h^{a-1}=\ds\lim_{h \to 0^+}h^{-\frac{1}{2}}=\ds\lim_{h \to 0^+}\frac{1}{\sqrt{h}}=\infty</m>.) Since <m>\sin\left(\frac{1}{x}\right)</m> goes back and forth between one and negative one,
<me>
\lim_{h \to 0^+}h^{a-1}\sin\left(\frac{1}{x}\right)=DNE</me>
</p>

<p>since as <m>h</m> goes to 0, the function oscillates between positive and negative numbers of ever-increasing magnitude.</p>
</li>
<li><p> If <m>a \gt 1</m>, then <m>a-1 \gt 0</m>, so <m>\ds\lim_{h \to 0^+}h^{a-1}=0</m>. Although <m>\sin\left(\frac{1}{x}\right)</m> oscillates wildly near <m>x=0</m>, it is bounded by <m>-1</m> and <m>1</m>. So,
<me>
(-1)h^{a-1} \leq h^{a-1}\sin\left(\frac{1}{h}\right) \leq h^{a-1}</me>
</p>

<p>Since both <m>\ds\lim_{h \to 0^+} (-1)h^{a-1}=0</m> and <m>\ds\lim_{h \to 0^+} h^{a-1}=0</m>, by the Squeeze Theorem, <me>
\ds\lim_{h \to 0^+} h^{a-1}\sin\left(\frac{1}{x}\right)=0</me>
 as well.</p>
</li>
</ul>
</p>

<p>In the above cases, we learned
<m>\ds\lim_{h \to 0^+}\frac{f(h)-f(0)}{h}=\ds\lim_{h \to 0^+} h^{a-1}\sin\left(\frac{1}{x}\right)=0</m> when <m>a \gt 1</m>, and </p>
<p>
<m>\ds\lim_{h \to 0^+}\frac{f(h)-f(0)}{h}=\ds\lim_{h \to 0^+} h^{a-1}\sin\left(\frac{1}{x}\right)\neq 0</m> when <m>a \leq 1</m>.</p>
<p>
So, <m>f</m> is differentiable at <m>x=0</m> if and only if <m>a \gt 1</m>.
</p></solution>
</exercise>

</exercisegroup>

</exercises>
